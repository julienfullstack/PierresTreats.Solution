@{
  Layout = "_Layout";
}

@model PierresTreats.Models.Treat
@using PierresTreats.Models


<h4>Confirm Deletion?</h4>
<div class="col">

 @using (Html.BeginForm("Delete", "Treats", new { id = Model.TreatId }, FormMethod.Post))
{
  <input type="submit" value="Delete Treat" onclick="return confirm('Are you sure you want to delete this Treat?')"/>
}

</div>

